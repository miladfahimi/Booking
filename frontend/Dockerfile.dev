# Development Container for Live Reloading with Nx Workspace
FROM node:18-alpine

# Set working directory inside the container
WORKDIR /app

# Copy package.json and package-lock.json for dependency installation
COPY tennis-time ./tennis-time/

# Install project dependencies
RUN cd tennis-time && npm install

# Set the working directory to the Nx workspace
WORKDIR /app/tennis-time

# Expose the port used by the Nx development server
EXPOSE 4200

# Default command to serve the frontend workspace with live reloading
CMD ["npx", "nx", "serve", "frontend", "--host", "0.0.0.0"]
